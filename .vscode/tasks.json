{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run start.bat",
            "type": "shell",
            "command": "${workspaceFolder}\\start.bat",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}